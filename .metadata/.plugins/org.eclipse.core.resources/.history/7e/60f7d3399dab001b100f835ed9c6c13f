package service;

import java.util.List;

public class CalculationService<T extends Comparable<T>> {
	
	static T max<T>(List<T> list){
		
		T max = list.get(0);
		for(T item : list)
			if(item.compareTo(max))
				max = item;
		
		return max;
	}	
